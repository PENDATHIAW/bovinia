# Déposez vos visuels officiels ici

Noms exacts attendus — voir `../README.md`.

Après dépôt : commit + push sur GitHub. Le script `sync-incoming-assets.mjs` les copie au bon emplacement avant chaque build.

**Ne pas déposer** `formulas-chart` ni images avec formules détaillées.
